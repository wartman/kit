package kit;

// @todo: The `spec` library should probably be its own
// package, but for now it's really handy to have it here.
typedef Assert = kit.spec.Assert;
@:noUsing typedef Runner = kit.spec.Runner;
@:noUsing typedef Suite = kit.spec.Suite;
@:noUsing typedef Spec = kit.spec.Spec;
@:noUsing typedef Reporter = kit.spec.Reporter;
